Drop client-supplied photos here, mirroring the `public/images/` structure but as `.jpg`/`.png`
(e.g. `assets-src/images/hero/hero-banana-leaf.jpg`). Then run `npm run images:convert` — each file is
resized to the size the site expects and written to `public/images/**` as WebP. This folder is git-ignored.
